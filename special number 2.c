153是一个非常特殊的数，它等于它每位数字的立方和，即153＝1*1*1+5*5*5+3*3*3。编程求所有满足这种条件的三位十进制数。
输出格式：按从小到大的顺序输出满足条件的三位十进制数。

#include <stdio.h>
int main()
{
	int i , j , k , n;
	for(n = 100; n < 1000; n++)
	{
		j = n / 100;
		j = n / 10 % 10;
		k = n % 10;
		if(i * 100 + j * 10 + k == i * i * i + j * j * j + k * k * k)
			printf ("%d\n",n);
	}
	return 0;
}