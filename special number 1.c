1221是一个非常特殊的数，它从左边读和右边读是一样的，编程求所有这样的四位十进制数。
输出格式：从小到大的顺序输出满足条件的四位十进制数。

#include <stdio.h>
int main()
{
	int a,b,c,d,i;
	for(i = 1000;i <= 9999;i++)
	{
		a = i / 1000;
		b = i /100 % 10;
		c = i /10 % 10;
		d = i %10;
		if(a == d && b == c)
			printf("%d\n",i);
	}
	return 0;
}